# Application config
spring.application.name=BigCityLibrary-RestServer-Repository-Test

# Active profile
spring.profiles.active=dev

# Spring console
spring.output.ansi.enabled=always

# LOGGING
logging.level.root=WARN
